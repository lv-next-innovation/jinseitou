import * as React from "react";
import * as Redux from "redux";

const Logo = (props: any) => (
  <div className="header__logo">
    <img src="./logo.png" className="header__logo__img" />
  </div>
);

export default Logo;
