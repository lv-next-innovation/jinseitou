import * as React from "react";
import Header from "./header/header";
import Main from "../containers/main";

const Top = (props: any) => (
  <div>
    <Header />
    <Main />
  </div>
);

export default Top;
